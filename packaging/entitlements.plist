<?xml version="1.0" encoding="UTF-8"?>
<!--
  Hardened runtime entitlements for the macOS build.

  PyInstaller's onefile bootloader unpacks the interpreter and its dylibs into a temporary
  folder and loads them from there. Under the hardened runtime that is blocked by default:
  library validation only allows code signed by the same team from the ORIGINAL location.
  Without this, the app is signed and notarized and still refuses to start.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
